import { ModuleData } from '../../interfaces/main';
import { entitySetRelationships, entitySets } from './data';

export const regionalModuleData: ModuleData = {
  entitySets: entitySets,
  entitySetRelationships: entitySetRelationships,
};
